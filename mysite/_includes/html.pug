doctype html
include setupValue
include mixin/functions
html(lang="ja")
  head
    meta(charset="utf-8")
    meta(name="viewport", content="width=device-width, initial-scale=1")
    include head/load

  body
    .base.app
      .base.width
        include body/header
        include components/lightbox
        include components/audio
        .toTop
          img(src="/images/svg/watakazeTop.svg" title="トップへ飛ぶ")
        include body/main
    #svg_data(style='display:none')
      #more_button !{svgData.mapList.get(`${svgData.path}/more_button.svg`)}
    include body/js.pug
