---
layout: "layout/page/default.pug"
type: "list"
title: "お知らせ"
index: 2
enable:
  date: true
tags:
  - note
  - category
  - list
  - notice
---

h1 #{title}
.flex.center
  - let pages = Array.from(search.pages("note notice article"));
  - pages = pages.map((item)=>{return item.data}).sort((a,b)=>a.index-b.index);
  each item in pages
    div
      h2.lulo
        a(href=`${item.url}`) #{item.title.toUpperCase()}
      h3 #{item.description}
