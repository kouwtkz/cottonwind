{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "detail": "ビルドする",
            "type": "shell",
            "command": "cd ../; deno task lume"
        },
        {
            "label": "update",
            "detail": "staticのみアップデート",
            "type": "shell",
            "command": "cd ../; deno run --allow-env --allow-read --allow-write ./_scripts/staticCopy.ts"
        }
    ]
}
